import React from 'react';
import ChatInterface from '../components/ChatInterface.tsx';

const ChatPage: React.FC = () => {
  return (
    <div className="flex-grow bg-[#fafafa] flex flex-col min-h-[calc(100vh-73px)] animate-in fade-in duration-500">
      <div className="max-w-6xl mx-auto w-full px-6 py-12 flex-grow flex flex-col lg:flex-row gap-8">
        {/* Sidebar Navigation & Intel */}
        <div className="lg:w-1/3 space-y-6">
          <div className="p-8 bg-zinc-950 rounded-3xl text-white shadow-2xl border border-white/5 relative overflow-hidden">
            <div className="absolute -right-10 -top-10 w-40 h-40 bg-blue-600/10 rounded-full blur-[80px]"></div>
            <div className="relative z-10">
              <h2 className="text-3xl font-[900] tracking-tighter uppercase mb-2 text-blue-400">Portal Scout</h2>
              <p className="text-zinc-400 serif-text italic text-lg leading-snug">
                Arizona's intelligent companion for health discovery and desert recovery.
              </p>
            </div>
            <div className="mt-8 pt-6 border-t border-white/10 space-y-3 relative z-10">
              <div className="flex items-center gap-3">
                <div className="w-1.5 h-1.5 bg-green-500 rounded-full"></div>
                <span className="text-[10px] font-black uppercase tracking-widest text-zinc-500">SageSuite Directory Active</span>
              </div>
              <div className="flex items-center gap-3">
                <div className="w-1.5 h-1.5 bg-blue-500 rounded-full animate-pulse"></div>
                <span className="text-[10px] font-black uppercase tracking-widest text-zinc-500">Live Trail Reports</span>
              </div>
            </div>
          </div>
          
          <div className="p-8 bg-white border border-zinc-200 rounded-3xl shadow-sm">
             <h4 className="text-[11px] font-black uppercase tracking-widest text-[#0d47a1] mb-6">Ask Scout About...</h4>
             <ul className="space-y-4">
               {[
                 "How to setup a sage subdomain",
                 "Vetted trail guides in Sedona",
                 "Local Arizona sports recovery",
                 "Connecting to GHL Communities"
               ].map((q, i) => (
                 <li key={i} className="text-sm font-bold text-zinc-500 hover:text-black cursor-pointer flex items-center gap-3 group transition-colors">
                   <div className="w-1 h-1 bg-zinc-200 group-hover:bg-blue-600 rounded-full transition-colors"></div>
                   {q}
                 </li>
               ))}
             </ul>
          </div>
        </div>

        {/* Focused Chat Interface */}
        <div className="lg:w-2/3 flex-grow flex flex-col bg-white border border-zinc-200 rounded-3xl overflow-hidden shadow-2xl shadow-blue-900/5">
           <ChatInterface className="h-full border-none shadow-none" initialMessage="Scout Portal Active. I'm connected to the SageSuite directory and local trail databases. How can I assist your high-desert journey today?" />
        </div>
      </div>
      
      <div className="pb-8 text-center">
        <p className="text-[9px] font-black uppercase tracking-[0.4em] text-zinc-300">
          Encrypted Connection • Health & Travels Scouting Division
        </p>
      </div>
    </div>
  );
};

export default ChatPage;
